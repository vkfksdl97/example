<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="ProfessorDepartmentMapper">
	<!-- Beans 클래스의 객체이름(id)과 클래스 이름(type)을 명시한다. -->
	<resultMap type="study.spring.simplespring.model.ProfessorDepartment" id="professorDepartmentMap">
		<!-- Beans의 멤버변수(property) 이름과 대상 테이블의 컬럼(column)을 연결한다. -->
		<result property="profno" column="profno" />
		<result property="name" column="name" />
		<result property="userid" column="userid" />
		<result property="position" column="position" />
		<result property="sal" column="sal" />
		<result property="hiredate" column="h_date" />
		<result property="comm" column="comm" />
		<result property="deptno" column="deptno" />
		<result property="dname" column="dname" />
		<result property="loc" column="loc" />
	</resultMap>
	
	<!-- Join 결과를 반환 -->
	<select id="selectJoin" parameterType="study.spring.simplespring.model.ProfessorDepartment"
		resultMap="professorDepartmentMap">
		Select profno, name, userid, position, sal, date_format(hiredate, '%Y-%m-%d') As h_date,
			   comm, p.deptno, dname, loc
		From professor As p
		Inner Join department As d
		On p.deptno = d.deptno
		
		<where>
			<if test="dname != null and dname != ''">
				dname Like concat('%', #{dname}, '%')
			</if>
			<if test="loc != null and loc != ''">
				Or loc Like concat('%', #{loc}, '%')
			</if>
		</where>
	</select>
</mapper>